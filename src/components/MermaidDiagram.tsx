import{useEffect,useId,useRef,useState}from'react';
export default function MermaidDiagram({definition,title}:{definition:string;title:string}){const ref=useRef<HTMLDivElement>(null);const id=useId().replaceAll(':','');const[error,setError]=useState('');useEffect(()=>{let active=true;(async()=>{try{const mermaid=(await import('mermaid')).default;mermaid.initialize({startOnLoad:false,securityLevel:'strict',theme:'base',themeVariables:{primaryColor:'#edf5ff',primaryTextColor:'#161616',primaryBorderColor:'#0f62fe',lineColor:'#525252',secondaryColor:'#d9fbfb',tertiaryColor:'#f4f4f4',fontFamily:'IBM Plex Sans, Arial'}});const{svg}=await mermaid.render('mermaid-'+id,definition);if(active&&ref.current)ref.current.innerHTML=svg}catch(e){if(active)setError(e instanceof Error?e.message:String(e))}})();return()=>{active=false}},[definition,id]);return <section className="mermaid-card" aria-label={title}><h2>{title}</h2>{error?<div className="warning">Unable to render diagram: {error}</div>:<div ref={ref} className="mermaid-diagram"/>}</section>}
